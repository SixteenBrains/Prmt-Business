import 'package:flutter/material.dart';

class PromotersTransactions extends StatelessWidget {
  const PromotersTransactions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'No Data Found',
        style: TextStyle(
          color: Colors.white,
          fontSize: 20.0,
        ),
      ),
    );
  }
}
